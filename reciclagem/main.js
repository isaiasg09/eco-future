function scrollar() {
  let destination = document.querySelector(".lixeiras1");

  // Scroll to the destination using
  // scrollIntoView method
  destination.scrollIntoView({
    behavior: "smooth",
  });
}
